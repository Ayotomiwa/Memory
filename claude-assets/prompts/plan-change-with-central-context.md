Plan this change using both the local repo context and the central engineering context.

Do not edit files yet.

Before proposing the plan:
1. Read the local `CLAUDE.md`.
2. Follow its required startup routing.
3. Read the repo guide listed in `Type`.
4. Read the repo page listed in `Repo page`, if it exists.
5. If the change crosses repos, read `atlas/dependency-map.md`.

Return:
- relevant context read
- assumptions and missing information
- implementation plan
- validation plan
- whether any durable learning should be captured in `staging/`
